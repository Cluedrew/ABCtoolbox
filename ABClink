#! /bin/bash
# Creates a link to the currant directory for easy access.

# Check for good input.
if [ $# != 2 ]; then
  exit 1
elif [ ! -d $1 ]; then

fi

### Alt:

# Creates a link from a directory searched by the g++ -l flag (/user/bin ?)
# to where the objects/headers for ABC Tool Box are.
